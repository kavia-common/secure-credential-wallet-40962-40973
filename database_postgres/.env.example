# Database (development uses SQLite file)
# The SQLite database file path used by the development environment.
# This should match the backend_api DB_URL path (sqlite:///./app.db -> ./app.db).
SQLITE_DB=./app.db
