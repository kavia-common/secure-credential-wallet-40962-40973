# Database (.env.example)
# Copy to .env for local development: cp .env.example .env

# SQLite database file used for development
# Must match backend_api/.env DB_URL path component (sqlite:///./app.db)
SQLITE_DB=./app.db
